# Runtime dependencies, pinned to the current macOS arm64 / Python 3.11 environment.
# Install: .venv/bin/python -m pip install -r requirement.txt
# PaddleOCR model weights and system Poppler tools are installed separately (README).
PyMuPDF==1.28.2
paddlepaddle==3.3.1
paddleocr==3.7.0
